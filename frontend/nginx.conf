server {
    listen 80;
    server_name ranx24.com www.ranx24.com;

    root /usr/share/nginx/html;
    index index.html;

    # React SPA Routing Fix
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }

    error_page 404 /index.html;

    # Backend API reverse proxy
    location /api/ {
        proxy_pass http://backend:5000/; # <--- CHANGE if service name different
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Socket.IO WebSockets reverse proxy
    location /socket.io/ {
        proxy_pass http://backend:5000/socket.io/; # <--- Same here
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Error handler
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
